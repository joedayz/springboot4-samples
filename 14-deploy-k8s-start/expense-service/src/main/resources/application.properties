springdoc.swagger-ui.path=/swagger-ui.html
server.port=8081

# Health probes para Kubernetes/Azure (liveness + readiness)
management.endpoint.health.probes.enabled=true
management.endpoints.web.exposure.include=health
